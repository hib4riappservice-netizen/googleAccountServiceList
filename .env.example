# Auth.js のセッション暗号化に使う秘密鍵。`npx auth secret` で生成する。
AUTH_SECRET=

# Google Cloud Console の OAuth 2.0 クライアント ID / シークレット
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Sentry（エラー監視）のDSN。秘密情報ではないが値をここに集約する。scripts/setup-sentry.sh で取得できる。
NEXT_PUBLIC_SENTRY_DSN=
